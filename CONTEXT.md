# CONTEXT

本專案的領域詞彙表。純 glossary，不含實作細節；實作請看程式碼與 `docs/adr/`。

## session

有兩個不同的意思，區分清楚才不會誤解程式碼與畫面。

- **使用者心智的 session**：「我在這個分頁打開對話到現在」。切換到別的頁面
  再切回來仍是同一個 session；重新整理或關閉分頁才歸零。
  畫面上的累計用量以此為邊界。
- **SDK 的 `session_id`**：Agent SDK 用來接續對話的識別碼，由 `options.resume`
  帶入。`resume` 有可能 **fork** 出新的 `session_id` —— 這是 SDK 的實作細節，
  不該漏到 UI；使用者看到的仍是同一個對話視窗、同一串訊息。

因此 `session_id` 變更時，累計用量**不歸零**。

## 用量 / usage

一段時間內消耗的 token 數量。同樣要區分兩個層級：

- **一輪的用量**：後端一次 `query()` 呼叫所消耗的量。後端以 `usage` 事件送出，
  排在 `done` 與 `error` 之前。
- **累計用量**：使用者心智的 session 內所有輪次的總和。由前端累加。

**失敗的輪次計入。** turn 用盡、API 過載等失敗結果一樣帶得回用量，
那些 token 確實已消耗，漏掉會低報。

**中斷的輪次不計入。** `query()` 被 abort 時 `result` 訊息不會產生，
用量無從取得。那些 token 實際上已消耗，但 SDK 拿不到數字，
故不計入、也不估算 —— 估算會是編造的。
代價是顯示的數字會略低於實際帳單。

## 四種 token 分類

四者計價完全不同，且三種輸入分類**互斥**，故一律分開呈現，不做總和。
標籤沿用 Anthropic 生態圈慣例。

| 標籤 | 意思 |
|---|---|
| `in` | 未命中快取的輸入 token（**不是**「總輸入」） |
| `cache_c` | 寫入快取的輸入 token（cache creation） |
| `cache_r` | 命中快取讀取的輸入 token（cache read） |
| `out` | 輸出 token |

`in` 的標籤字面與實際語意有落差，目前受眾為開發者故可接受；
若日後改給非開發者看，標籤應重新檢討。
